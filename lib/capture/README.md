# lib/capture — 相机采集与编排

负责设备相机的采集与编排：

- 打开/配置相机，按平台能力请求高帧率模式（目标 240fps，平台差异见 `platform/`）。
- 将帧序列以稳定接口（时间戳 + 帧缓冲引用）交给 `lib/session`。
- 管理采集生命周期（开始/暂停/停止）与丢帧统计。

本目录不含任何算法逻辑：采集到的帧一律经 `lib/runtime` 送入 batana-runtime 处理。
